{
	"name": "@avunu/wordpress-edge-worker",
	"private": true,
	"version": "0.1.0",
	"description": "The platform edge Worker for WordPress-on-Cloudflare sites: page cache, hardening guard, authenticated D1 proxy, and the container class. Bundled per platform version by lib.mkSiteWorker.",
	"type": "module",
	"scripts": {
		"test": "vitest run"
	},
	"dependencies": {
		"@cloudflare/containers": "0.3.7"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.20.3",
		"vitest": "~4.1.10",
		"wrangler": "^4.20.0"
	}
}
