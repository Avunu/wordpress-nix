# Secrets for the dev shell (S3_KEY, JWT_AUTH_CLIENT_SECRET, ...): a gitignored .env,
# defined into wp-config.php by wordpress-nix.environmentConstants. Optional.
dotenv_if_exists .env
use flake . --no-pure-eval
